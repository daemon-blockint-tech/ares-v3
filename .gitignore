# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
# Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# ARES specific outputs
ares-output/
ares-benchmark-report.json
ares-benchmark-report.md
ares-dashboard.html
poc/
logs/
*.log

# Dataset (managed via Git LFS, so we ignore the directory but not the LFS pointers)
# Actually, if we use LFS we don't ignore it. We let LFS handle it.
# We will just not put dataset/ in gitignore. We will track it.
# Wait, the user said "Git LFS (Recommended)" for dataset. So dataset shouldn't be gitignored.
# Removing dataset/ from gitignore.

# Environments
.env
.env.local

# IDEs
.idea/
.vscode/
*.swp
*.swo

# OS specific
.DS_Store
Thumbs.db

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
docs/paper/ieee-style/main.pdf
